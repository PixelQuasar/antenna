[package]
name = "antenna-integration-tests"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
antenna = { path = "../antenna", default-features = false, features = ["native"] }
antenna-protocol.workspace = true

tokio.workspace = true
serde.workspace = true
anyhow.workspace = true
tempfile.workspace = true
